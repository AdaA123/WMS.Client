<UserControl x:Class="WMS.Client.Views.FinancialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1000">

    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="财务分析" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <DatePicker SelectedDate="{Binding StartDate}" materialDesign:HintAssist.Hint="开始日期" Width="100" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <TextBlock Text="-" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding EndDate}" materialDesign:HintAssist.Hint="结束日期" Width="100" Margin="0,0,20,0" VerticalAlignment="Center"/>
            <Button Command="{Binding RefreshDataCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Content="查询" Margin="0,0,20,0"/>
            <Button Command="{Binding PrintCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Content="打印当前报表" Margin="0,0,10,0"/>
            <Button Command="{Binding ExportCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Content="导出Excel"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <materialDesign:Card Grid.Column="0" Background="#4CAF50" Foreground="White" Padding="20" Margin="0,0,10,0" UniformCornerRadius="8">
                <StackPanel>
                    <TextBlock Text="总收入 (销售)" FontSize="14" Opacity="0.9"/>
                    <TextBlock Text="{Binding TotalRevenue, StringFormat=C2}" FontSize="28" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </materialDesign:Card>
            <materialDesign:Card Grid.Column="1" Background="#F44336" Foreground="White" Padding="20" Margin="5,0,5,0" UniformCornerRadius="8">
                <StackPanel>
                    <TextBlock Text="总成本 (采购)" FontSize="14" Opacity="0.9"/>
                    <TextBlock Text="{Binding TotalCost, StringFormat=C2}" FontSize="28" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </materialDesign:Card>
            <materialDesign:Card Grid.Column="2" Background="#2196F3" Foreground="White" Padding="20" Margin="10,0,0,0" UniformCornerRadius="8">
                <StackPanel>
                    <TextBlock Text="毛利润 (净收益)" FontSize="14" Opacity="0.9"/>
                    <TextBlock Text="{Binding TotalGrossProfit, StringFormat=C2}" FontSize="28" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <TabControl Grid.Row="2" SelectedIndex="{Binding SelectedTabIndex}" Style="{StaticResource MaterialDesignFilledTabControl}" materialDesign:ColorZoneAssist.Mode="Standard">

            <TabItem Header="单品利润分析">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
                        <materialDesign:PackIcon Kind="Magnify" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="Gray"/>
                        <TextBox Width="200" materialDesign:HintAssist.Hint="搜索产品名称..." Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding FinancialList}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Style="{StaticResource MaterialDesignDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="产品名称" Binding="{Binding ProductName}" Width="*" FontWeight="Bold"/>
                            <DataGridTextColumn Header="采购总成本" Binding="{Binding TotalCost, StringFormat=C2}"/>
                            <DataGridTextColumn Header="销售总收入" Binding="{Binding TotalRevenue, StringFormat=C2}"/>
                            <DataGridTextColumn Header="退款总额" Binding="{Binding TotalRefund, StringFormat=C2}" Foreground="Red"/>
                            <DataGridTextColumn Header="毛利/结余" Binding="{Binding GrossProfit, StringFormat=C2}" FontWeight="Bold">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsProfitPositive}" Value="False">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsProfitPositive}" Value="True">
                                                <Setter Property="Foreground" Value="Green"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTemplateColumn Header="操作" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="查看进销存详情"
                                                Command="{Binding DataContext.ViewDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                CommandParameter="{Binding}">
                                            <materialDesign:PackIcon Kind="ChartBox" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="月度经营报表">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <materialDesign:Card Margin="10" Padding="10">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBlock Text="图表视图" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <ToggleButton IsChecked="{Binding IsChartExpanded}"/>
                            </StackPanel>
                            <lvc:CartesianChart Series="{Binding ChartSeries}" Height="200" LegendLocation="Right" 
                                                Visibility="{Binding IsChartExpanded, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis Title="月份" Labels="{Binding ChartLabels}"/>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis Title="金额" LabelFormatter="{Binding YFormatter}"/>
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>
                        </StackPanel>
                    </materialDesign:Card>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding MonthlyList}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Style="{StaticResource MaterialDesignDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="月份" Binding="{Binding PeriodName}" FontWeight="Bold"/>
                            <DataGridTextColumn Header="总收入" Binding="{Binding Revenue, StringFormat=C2}"/>
                            <DataGridTextColumn Header="总成本" Binding="{Binding Cost, StringFormat=C2}"/>
                            <DataGridTextColumn Header="总退款" Binding="{Binding Refund, StringFormat=C2}"/>
                            <DataGridTextColumn Header="净利润" Binding="{Binding Profit, StringFormat=C2}" FontWeight="Bold" Foreground="#2196F3"/>
                            <DataGridTemplateColumn Header="操作" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="查看月度流水"
                                                Command="{Binding DataContext.ViewPeriodDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                CommandParameter="{Binding}">
                                            <materialDesign:PackIcon Kind="Magnify" Foreground="Gray"/>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="年度经营报表">
                <DataGrid ItemsSource="{Binding YearlyList}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Margin="0,10,0,0" Style="{StaticResource MaterialDesignDataGrid}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="年份" Binding="{Binding PeriodName}" FontWeight="Bold"/>
                        <DataGridTextColumn Header="总收入" Binding="{Binding Revenue, StringFormat=C2}"/>
                        <DataGridTextColumn Header="总成本" Binding="{Binding Cost, StringFormat=C2}"/>
                        <DataGridTextColumn Header="总退款" Binding="{Binding Refund, StringFormat=C2}"/>
                        <DataGridTextColumn Header="净利润" Binding="{Binding Profit, StringFormat=C2}" FontWeight="Bold" Foreground="#2196F3"/>
                        <DataGridTemplateColumn Header="操作" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource MaterialDesignIconButton}" ToolTip="查看年度流水"
                                            Command="{Binding DataContext.ViewPeriodDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}">
                                        <materialDesign:PackIcon Kind="Magnify" Foreground="Gray"/>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>