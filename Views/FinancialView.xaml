<UserControl x:Class="WMS.Client.Views.FinancialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <Style x:Key="CenterText" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <DataTemplate x:Key="FinancialDetailsTemplate">
            <Border Background="#F5F5F5" Padding="10" BorderBrush="#E0E0E0" BorderThickness="0,1,0,1">
                <StackPanel>
                    <TextBlock Text="该时段产品明细：" FontWeight="Bold" Margin="0,0,0,5" Foreground="Gray"/>
                    <DataGrid ItemsSource="{Binding Details}" 
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" SelectionMode="Single"
                              Background="White" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="产品名称" Binding="{Binding ProductName}" Width="200" FontWeight="Medium"/>
                            <DataGridTextColumn Header="收入" Binding="{Binding Revenue, StringFormat=C2}" Width="100" Foreground="#4CAF50"/>
                            <DataGridTextColumn Header="成本" Binding="{Binding Cost, StringFormat=C2}" Width="100" Foreground="#F44336"/>
                            <DataGridTextColumn Header="退款" Binding="{Binding Refund, StringFormat=C2}" Width="80"/>
                            <DataGridTextColumn Header="贡献毛利" Binding="{Binding Profit, StringFormat=C2}" Width="100" FontWeight="Bold" Foreground="#3F51B5"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="财务收支概览" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <materialDesign:Card Grid.Column="0" Margin="0,0,10,0" Padding="20" UniformCornerRadius="8" Background="#4CAF50">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <materialDesign:PackIcon Kind="CashMultiple" Width="30" Height="30" Foreground="White" VerticalAlignment="Center"/>
                        <TextBlock Text="销售总收入" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TotalRevenue, StringFormat=C2}" Foreground="White" FontSize="28" FontWeight="Bold" HorizontalAlignment="Right"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="1" Margin="10,0,10,0" Padding="20" UniformCornerRadius="8" Background="#F44336">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <materialDesign:PackIcon Kind="CartOutline" Width="30" Height="30" Foreground="White" VerticalAlignment="Center"/>
                        <TextBlock Text="采购总成本" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TotalCost, StringFormat=C2}" Foreground="White" FontSize="28" FontWeight="Bold" HorizontalAlignment="Right"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="2" Margin="10,0,0,0" Padding="20" UniformCornerRadius="8" Background="#2196F3">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <materialDesign:PackIcon Kind="ChartLine" Width="30" Height="30" Foreground="White" VerticalAlignment="Center"/>
                        <TextBlock Text="总毛利 (结余)" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TotalGrossProfit, StringFormat=C2}" Foreground="White" FontSize="28" FontWeight="Bold" HorizontalAlignment="Right"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <materialDesign:Card Grid.Row="2" Padding="0" UniformCornerRadius="8" VerticalAlignment="Stretch">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="20,15,20,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="CalendarRange" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="Gray"/>

                        <DatePicker SelectedDate="{Binding StartDate}" Width="100" 
                                    materialDesign:HintAssist.Hint="开始日期" 
                                    VerticalAlignment="Center"/>

                        <TextBlock Text=" - " VerticalAlignment="Center" Margin="5,0"/>

                        <DatePicker SelectedDate="{Binding EndDate}" Width="100" 
                                    materialDesign:HintAssist.Hint="结束日期" 
                                    VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="查询/刷新" Command="{Binding RefreshDataCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,15,0"/>

                        <Button Command="{Binding ExportCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileExcel" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                <TextBlock Text="导出Excel" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Content="打印报表" Command="{Binding PrintCommand}" Style="{StaticResource MaterialDesignOutlinedButton}"/>
                    </StackPanel>
                </Grid>

                <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}" Margin="5">
                    <TabItem Header="单品利润分析" FontSize="16">
                        <DataGrid ItemsSource="{Binding FinancialList}" 
                                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                                  HeadersVisibility="All" SelectionMode="Single"
                                  Style="{StaticResource MaterialDesignDataGrid}">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="产品名称" Binding="{Binding ProductName}" Width="2*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Left" />
                                            <Setter Property="Padding" Value="10,0,0,0"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="采购总成本" Binding="{Binding TotalCost, StringFormat=C2}" Width="1*" ElementStyle="{StaticResource CenterText}"/>
                                <DataGridTextColumn Header="销售总收入" Binding="{Binding TotalRevenue, StringFormat=C2}" Width="1*" ElementStyle="{StaticResource CenterText}"/>
                                <DataGridTextColumn Header="退款总额" Binding="{Binding TotalRefund, StringFormat=C2}" Width="1*" ElementStyle="{StaticResource CenterText}"/>
                                <DataGridTextColumn Header="毛利润" Binding="{Binding GrossProfit, StringFormat=C2}" Width="1*" FontWeight="Bold">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center" />
                                            <Setter Property="VerticalAlignment" Value="Center" />
                                            <Setter Property="Foreground" Value="#3F51B5"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>

                    <TabItem Header="月度报表" FontSize="16">
                        <DataGrid ItemsSource="{Binding MonthlyList}" 
                                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                                  HeadersVisibility="All" SelectionMode="Single"
                                  RowDetailsTemplate="{StaticResource FinancialDetailsTemplate}"
                                  RowDetailsVisibilityMode="VisibleWhenSelected"
                                  Style="{StaticResource MaterialDesignDataGrid}">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="月份 (点击查看详情)" Binding="{Binding PeriodName}" Width="1.5*" FontWeight="Bold" ElementStyle="{StaticResource CenterText}"/>
                                <DataGridTextColumn Header="总收入" Binding="{Binding Revenue, StringFormat=C2}" Width="1*" ElementStyle="{StaticResource CenterText}" Foreground="#4CAF50"/>
                                <DataGridTextColumn Header="总成本" Binding="{Binding Cost, StringFormat=C2}" Width="1*" ElementStyle="{StaticResource CenterText}" Foreground="#F44336"/>
                                <DataGridTextColumn Header="总退款" Binding="{Binding Refund, StringFormat=C2}" Width="1*" ElementStyle="{StaticResource CenterText}" Foreground="Gray"/>
                                <DataGridTextColumn Header="净利润" Binding="{Binding Profit, StringFormat=C2}" Width="1*" FontWeight="Bold">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center" />
                                            <Setter Property="VerticalAlignment" Value="Center" />
                                            <Setter Property="Foreground" Value="#3F51B5"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="利润率" Binding="{Binding ProfitMargin}" Width="0.8*" ElementStyle="{StaticResource CenterText}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>

                    <TabItem Header="年度报表" FontSize="16">
                        <DataGrid ItemsSource="{Binding YearlyList}" 
                                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                                  HeadersVisibility="All" SelectionMode="Single"
                                  RowDetailsTemplate="{StaticResource FinancialDetailsTemplate}"
                                  RowDetailsVisibilityMode="VisibleWhenSelected"
                                  Style="{StaticResource MaterialDesignDataGrid}">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="年份 (点击查看详情)" Binding="{Binding PeriodName}" Width="1.5*" FontWeight="Bold" ElementStyle="{StaticResource CenterText}"/>
                                <DataGridTextColumn Header="总收入" Binding="{Binding Revenue, StringFormat=C2}" Width="1*" ElementStyle="{StaticResource CenterText}" Foreground="#4CAF50"/>
                                <DataGridTextColumn Header="总成本" Binding="{Binding Cost, StringFormat=C2}" Width="1*" ElementStyle="{StaticResource CenterText}" Foreground="#F44336"/>
                                <DataGridTextColumn Header="总退款" Binding="{Binding Refund, StringFormat=C2}" Width="1*" ElementStyle="{StaticResource CenterText}" Foreground="Gray"/>
                                <DataGridTextColumn Header="净利润" Binding="{Binding Profit, StringFormat=C2}" Width="1*" FontWeight="Bold">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center" />
                                            <Setter Property="VerticalAlignment" Value="Center" />
                                            <Setter Property="Foreground" Value="#3F51B5"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="利润率" Binding="{Binding ProfitMargin}" Width="0.8*" ElementStyle="{StaticResource CenterText}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                </TabControl>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>