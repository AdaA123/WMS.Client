<UserControl x:Class="WMS.Client.Views.ForgotPasswordDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="350" Background="White">
    <Grid Margin="20">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <materialDesign:PackIcon Kind="LockReset" Width="24" Height="24" Foreground="{StaticResource PrimaryHueMidBrush}" VerticalAlignment="Center"/>
                <TextBlock Text="重置密码" FontSize="18" FontWeight="Bold" Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBox Text="{Binding ResetUsername, UpdateSourceTrigger=PropertyChanged}" 
                     materialDesign:HintAssist.Hint="请输入账号 (默认 admin)" 
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                     Margin="0,0,0,15"/>

            <TextBlock Text="密保问题：" Foreground="Gray" FontSize="12" Margin="0,5,0,0"/>
            <TextBlock Text="{Binding ResetSecurityQuestion}" FontSize="14" FontWeight="Medium" Margin="0,0,0,15" TextWrapping="Wrap"/>

            <TextBox Text="{Binding ResetAnswer}" 
                     materialDesign:HintAssist.Hint="请输入密保答案" 
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                     Margin="0,0,0,15"/>

            <PasswordBox x:Name="PbNewPassword"
                         materialDesign:HintAssist.Hint="请输入新密码"
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                         Margin="0,0,0,20"/>

            <TextBlock Text="* 默认admin账号的答案是 **" Foreground="Gray" FontSize="11" Margin="0,-10,0,10"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Style="{StaticResource MaterialDesignFlatButton}" 
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" 
                        CommandParameter="False"/>

                <Button Content="确认重置" Style="{StaticResource MaterialDesignRaisedButton}" Margin="10,0,0,0"
                        Command="{Binding ExecuteResetPasswordCommand}" 
                        CommandParameter="{Binding ElementName=PbNewPassword}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>