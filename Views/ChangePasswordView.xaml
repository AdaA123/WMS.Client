<Window x:Class="WMS.Client.Views.ChangePasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="修改密码" Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <materialDesign:Card UniformCornerRadius="10" Margin="10" materialDesign:ElevationAssist.Elevation="Dp6">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="修改密码" FontSize="20" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <PasswordBox Grid.Row="1" x:Name="PbOld"
                         materialDesign:HintAssist.Hint="请输入旧密码"
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                         Margin="0,10"/>

            <PasswordBox Grid.Row="2" x:Name="PbNew"
                         materialDesign:HintAssist.Hint="请输入新密码"
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                         Margin="0,10"/>

            <PasswordBox Grid.Row="3" x:Name="PbConfirm"
                         materialDesign:HintAssist.Hint="再次确认新密码"
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                         Margin="0,10"/>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
                <Button Content="取消" Style="{StaticResource MaterialDesignFlatButton}"
                        Command="{Binding CancelCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Margin="0,0,10,0"/>

                <Button Content="确认修改" 
                        Command="{Binding ConfirmCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>