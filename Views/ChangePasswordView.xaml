<Window x:Class="WMS.Client.Views.ChangePasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" 
        Title="修改密码" Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <materialDesign:PackIcon Kind="KeyChange" Width="40" Height="40" 
                                     HorizontalAlignment="Center" Margin="0,0,0,10" 
                                     Foreground="{DynamicResource PrimaryHueMidBrush}"/>

            <TextBlock Text="修改密码" FontSize="20" FontWeight="Bold" Margin="0,0,0,30" HorizontalAlignment="Center"/>

            <PasswordBox x:Name="OldPass"
                         materialDesign:HintAssist.Hint="旧密码"
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                         Margin="0,0,0,20"/>

            <PasswordBox x:Name="NewPass"
                         materialDesign:HintAssist.Hint="新密码"
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                         Margin="0,0,0,20"/>

            <PasswordBox x:Name="ConfirmPass"
                         materialDesign:HintAssist.Hint="确认新密码"
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                         Margin="0,0,0,40"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="确认修改" Command="{Binding ChangeCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,20,0" Width="100"/>

                <Button Content="取消" Command="{Binding CancelCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Width="100"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>