<UserControl x:Class="WMS.Client.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="仓库概览" 
                   Style="{StaticResource MaterialDesignHeadline4TextBlock}" 
                   Margin="0,0,0,30"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <materialDesign:Card Grid.Column="0" Margin="0,0,10,0" Padding="20" Background="#E3F2FD">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <materialDesign:PackIcon Kind="Dolly" Width="40" Height="40" Foreground="#1565C0"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TotalInbound}" FontSize="40" FontWeight="Bold" Foreground="#1565C0"/>
                    <TextBlock Text="入库总件数" FontSize="16" Foreground="Gray"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="1" Margin="10,0,10,0" Padding="20" Background="#FFEBEE">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <materialDesign:PackIcon Kind="TruckDelivery" Width="40" Height="40" Foreground="#C62828"/>
                    </StackPanel>
                    <TextBlock Text="{Binding TotalOutbound}" FontSize="40" FontWeight="Bold" Foreground="#C62828"/>
                    <TextBlock Text="出库总件数" FontSize="16" Foreground="Gray"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="2" Margin="10,0,0,0" Padding="20" Background="#E8F5E9">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <materialDesign:PackIcon Kind="Cube" Width="40" Height="40" Foreground="#2E7D32"/>
                    </StackPanel>
                    <TextBlock Text="{Binding CurrentStock}" FontSize="40" FontWeight="Bold" Foreground="#2E7D32"/>
                    <TextBlock Text="当前库存结余" FontSize="16" Foreground="Gray"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <materialDesign:PackIcon Grid.Row="2" Kind="Warehouse" 
                                 HorizontalAlignment="Center" VerticalAlignment="Center"
                                 Width="200" Height="200" Foreground="#EEEEEE"/>
    </Grid>
</UserControl>