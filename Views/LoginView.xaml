<Window x:Class="WMS.Client.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="登录" Height="460" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <materialDesign:Card Background="White" 
                     UniformCornerRadius="10" 
                     Margin="10"
                     materialDesign:ElevationAssist.Elevation="Dp6">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,40,0,30" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="Warehouse" Width="50" Height="50" 
                                         Foreground="{DynamicResource PrimaryHueMidBrush}" 
                                         HorizontalAlignment="Center"/>
                <TextBlock Text="WMS 仓储系统" 
                           FontSize="24" FontWeight="Bold" 
                           Margin="0,15,0,0"
                           Foreground="#333"/>
            </StackPanel>

            <TextBox Grid.Row="1" Margin="40,10"
                     materialDesign:HintAssist.Hint="请输入账号"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="16">
                <materialDesign:HintAssist.Foreground>
                    <SolidColorBrush Color="#DD000000" />
                </materialDesign:HintAssist.Foreground>
            </TextBox>

            <PasswordBox Grid.Row="2" x:Name="TxtPassword" Margin="40,20,40,10"
                         materialDesign:HintAssist.Hint="请输入密码"
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                         FontSize="16"/>

            <Button Grid.Row="3" Margin="40,30,40,10" Height="45"
                    Content="登  录"
                    FontSize="18"
                    Command="{Binding LoginCommand}"
                    CommandParameter="{Binding ElementName=TxtPassword}"
                    Style="{StaticResource MaterialDesignRaisedDarkButton}"
                    materialDesign:ButtonAssist.CornerRadius="5"/>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20">
                <Button Content="退出程序" 
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Foreground="Gray"
                        Command="{Binding ExitCommand}"/>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>