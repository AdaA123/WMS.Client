<Window x:Class="WMS.Client.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" 
        Height="500" Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF673AB7" Offset="0"/>
                <GradientStop Color="#FF512DA8" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <materialDesign:Card Width="350" Padding="25" UniformCornerRadius="10" 
                             VerticalAlignment="Center" HorizontalAlignment="Center"
                             Background="White" materialDesign:ElevationAssist.Elevation="Dp8">
            <StackPanel>
                <materialDesign:PackIcon Kind="Warehouse" Width="50" Height="50" 
                                         HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                <TextBlock Text="用户登录" FontSize="24" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,15,0,30" Foreground="#444"/>

                <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         materialDesign:HintAssist.Hint="请输入用户名"
                         materialDesign:HintAssist.Foreground="{DynamicResource PrimaryHueMidBrush}"
                         Text="{Binding Username}" Margin="0,0,0,20" FontSize="16"/>

                <PasswordBox x:Name="PasswordBox"
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                             materialDesign:HintAssist.Hint="请输入密码"
                             materialDesign:HintAssist.Foreground="{DynamicResource PrimaryHueMidBrush}"
                             Margin="0,0,0,30" FontSize="16"/>

                <Button Content="登录" Command="{Binding LoginCommand}" 
                        CommandParameter="{Binding ElementName=PasswordBox}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Height="45" FontSize="16" Margin="0,0,0,15"/>

                <Button Content="退出" Command="{Binding ExitCommand}" 
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Height="45" FontSize="16"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</Window>