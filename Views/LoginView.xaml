<UserControl x:Class="WMS.Client.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">

    <materialDesign:DialogHost Identifier="LoginDialogHost" DialogTheme="Inherit">
        <Grid>
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#C7EDCC" Offset="0.0"/>
                    <GradientStop Color="#1976D2" Offset="1.0"/>
                </LinearGradientBrush>
            </Grid.Background>

            <materialDesign:Card Width="350" Padding="30" UniformCornerRadius="10" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <materialDesign:PackIcon Kind="Warehouse" Width="60" Height="60" HorizontalAlignment="Center" Foreground="{StaticResource PrimaryHueMidBrush}" Margin="0,0,0,20"/>
                    <TextBlock Text="WMS 仓储管理系统" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                    <TextBox materialDesign:HintAssist.Hint="用户名" 
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                             Text="{Binding Username}" 
                             Margin="0,0,0,20" FontSize="16">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>

                    <PasswordBox x:Name="PasswordBox" 
                                 materialDesign:HintAssist.Hint="密码" 
                                 Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" 
                                 Margin="0,0,0,30" FontSize="16">
                        <materialDesign:PasswordBoxAssist.Password>
                            <Binding Path="Password" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"/>
                        </materialDesign:PasswordBoxAssist.Password>
                    </PasswordBox>

                    <Button Content="登 录" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PasswordBox}" Style="{StaticResource MaterialDesignRaisedButton}" Height="40" FontSize="16"/>

                    <Button Content="忘记密码？" 
                            Command="{Binding OpenForgotPasswordCommand}" 
                            Style="{StaticResource MaterialDesignFlatButton}" 
                            Height="30" 
                            FontSize="12" 
                            Foreground="Gray" 
                            Margin="0,10,0,0"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </materialDesign:DialogHost>
</UserControl>