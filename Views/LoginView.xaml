<UserControl x:Class="WMS.Client.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:WMS.Client.ViewModels">
    <Grid Background="#F0F2F5">
        <materialDesign:Card Width="350" Padding="30" VerticalAlignment="Center" UniformCornerRadius="10">
            <StackPanel>
                <TextBlock Text="WMS 系统登录" Style="{StaticResource MaterialDesignHeadline5TextBlock}" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <TextBox Text="{Binding UserName}" materialDesign:HintAssist.Hint="账号 (admin)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,10"/>
                <PasswordBox x:Name="PwdBox" materialDesign:HintAssist.Hint="密码 (123)" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0,10"/>
                <Button Content="登 录" Command="{Binding LoginCommand}" Margin="0,20,0,0"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>